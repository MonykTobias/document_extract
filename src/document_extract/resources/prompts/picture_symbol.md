This is a small inline symbol, badge, or icon that sits inside a table cell of a report.
Answer with the exact visible text on it (for example `S3` or `E1`).
If it carries no text, answer with a 1–3 word name of the symbol (for example
`checkmark`, `red dot`). Answer with the value only — no sentence, no description.
